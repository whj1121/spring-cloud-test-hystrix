server:
  port: 8084
eureka:
  client:
    service-url:
     defaultZone: http://localhost:8080/eureka/
spring:
  application:
    name: feign-hystrix
feign:
  hystrix:
    enabled: true
management:
  endpoints:
   web:
    exposure:
     include: "*"